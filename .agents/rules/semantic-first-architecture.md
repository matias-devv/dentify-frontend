---
trigger: always_on
---

- Use **semantic HTML5 elements** (`header`, `nav`, `main`, `section`, `article`, `aside`, `footer`, etc.).
- Use correct landmark roles only when native semantics are insufficient.
- Maintain a logical heading hierarchy (`h1` → `h6`) with no skips.
- Avoid unnecessary `div`/`span` wrappers.
- Structure markup for clarity, SEO, and accessibility.
